export enum PermissionsTypes {
  CONNECT = 'CONNECT',
  SPEAK = 'SPEAK',
}

export enum CommandsAvailable {
  play = 'play',
  stop = 'stop',
  skip = 'skip',
  shuffle = 'shuffle',
  lyrics = 'lyrics',
  playlist = 'playlist',
}
